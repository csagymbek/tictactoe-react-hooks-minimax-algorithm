import React, {useState} from 'react'
import styled from "styled-components";
import {DIMS, SQUARE_DIMS, DRAW, PLAYER_X, PLAYER_O} from "../constants";

const squares = new Array(DIMS ** 2).fill(null);

const Game = () => {
    return (
        <div>
            
        </div>
    )
}

export default Game
